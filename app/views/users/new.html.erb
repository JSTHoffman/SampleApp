<h1>Sign Up</h1>

<%= form_for(@user) do |form| %>
	<%= render 'shared/error_messages', :object => form.object %>
		<%= render 'fields', :form => form %>
	<div class="action">
		<%= form.submit "Sign Up" %>
	</div>
	<% end %>